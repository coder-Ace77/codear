FROM python:3.10-slim

# Install 'time' package
# Use apt-get for Debian-based images (python:3.10-slim is Debian based)
RUN apt-get update && apt-get install -y time && rm -rf /var/lib/apt/lists/*

WORKDIR /app
